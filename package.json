{
  "name": "elasticsearch-browser",
  "version": "0.0.1",
  "description": "Seperate repo for maintaining the elasticsearch browser build",
  "main": " ",
  "scripts": {
    "test": "echo \"tests are specified and run from the elasticsearch/elasticsearch-js project\" && exit 0"
  },
  "keywords": [
    "elasticsearch",
    "browser"
  ],
  "author": "Spencer Alger",
  "license": "Apache 2.0",
  "devDependencies": {
    "elasticsearch": "https://spenceralger.com/esjs-tarball"
  },
  "dependencies": {},
  "scripts": {
    "generate": "npm install && cd node_modules/elasticsearch-js && npm install && node scripts/export_client --outputDir=../../ && cd ../../"
  }
}
